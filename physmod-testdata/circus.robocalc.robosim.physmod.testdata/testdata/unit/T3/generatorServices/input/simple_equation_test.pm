pmodel SimpleEquationTest {
    // Minimal model for testing simple equation creation
    local a: real
    local b: real
    local c: real
    
    // Test equation: a = b + c
    local equation test1: a = b + c
}
